[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "aurora-method-builder"
version = "0.1.0"
description = "desktop editor for cycling protocols and palmsens methodscript"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PySide6",
    "aurora-unicycler @ git+https://github.com/Laswer5/aurora-unicycler.git@3d4f7e477389d9e32774e657bee42982e95fe4e5",
]

[project.gui-scripts]
aurora-method-builder = "aurora_method_builder.app:main"

[tool.setuptools.packages.find]
where = ["src"]
